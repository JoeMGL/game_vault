name: gamevault
description: A sleek tracker for physical video game collections
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  # state management (uncomment your preference)
  # flutter_riverpod: ^2.5.1
  # provider: ^6.1.2
  # percent_indicator: ^4.2.3
  # google_fonts: ^6.2.1
  # intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/
